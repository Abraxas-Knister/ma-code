#ifndef STATE_HPP
#define STATE_HPP
#include "config.hpp"

// struct Statevector
/* has a C operator
 * needs a Hilbertspace it depends on to initialise
 */

// struct Hilbertspace
/* stores the base and the numbers defining it
 * must be instantiated by Statevector()
 */

// void preparestate(Statevector&);

#endif
